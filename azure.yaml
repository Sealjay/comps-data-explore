# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: ava-gpt4-search
services:
  frontend:
    project: ./src
    dist: dist
    language: js
    host: staticwebapp
  backend:
    project: ./api
    language: py
    host: function
